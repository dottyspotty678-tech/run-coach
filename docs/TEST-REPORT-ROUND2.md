# Run Coach — stress test report, round 2 (verification pass)

Date: 23 Aug 2026, ~20:55 Europe/London (Sunday evening — the boundary-flip window, which
turned out to be a useful accident: both the ending week's legacy plan and the fresh
structured plan are observable at once).
Target: https://run-coach-fawn.vercel.app — fix commits `33e1427` (backend) and `2c82f9b` (UI)
deployed; fix-round migration run; fresh structured plan generated by the orchestrator
(200 in ~28 s, `week_start_date 2026-08-24`) shortly before this pass.
Method: as round 1 — `curl` + PIN session cookie; source cross-referenced read-only.
Generations spent this round: **0** (fresh plan already live; nothing required another call).

---

## 1. Round-1 findings — verification

### C-1 (Critical) — plan generation 500 → **FIXED**

- Evidence: `/plan` shows the full structured week for 2026-08-24 with "Generated 1 min ago";
  the orchestrator's generation returned 200 in ~28 s (vs two 50 s 500s in round 1). Source
  shows the rewritten `lib/weeklyPlan.ts` (coercion-based validation with warn-not-fail soft
  checks, date-matching by map rather than strict index equality) — the round-1 failure mode
  (validation failing twice → throw) is designed out. Not regenerated again this round per
  budget; the single successful production run plus the rendered output is sufficient proof.

### M-1 (Major) — old-format fallback everywhere → **FIXED**

- `/plan`: week summary at top ("Base phase build-back… roughly 20 km running… one long run
  and two gym strength sessions"), phase chip ("base", "32 weeks to race"), Regenerate +
  "Generated 1 min ago", then **7 day cards** (Mon 24 Aug – Sun 30 Aug) each with session
  badge (Rest / Easy run / Strength / Long run), title, detail, why, duration, Travel
  indicator — plus the calendar context strip and "Full calendar" link.
- `/food`: Meals ⇄ Shopping list control; 7 day cards with meal-type badges (Eating out /
  Home), prep chip ("20 min") and Method disclosure on the home meal; **categorised shopping
  list** (fruit & veg / meat & fish / store cupboard) with qualitative quantities ("1 bag",
  "small punnet", "6-8 pieces") and a "9 of 9 to get" counter.
- `/` (Today): week strip flipped to next week with per-day session abbreviations
  (M Rest · T Easy · W Str · T Rest · F Str · S Long · S Easy) and run-only snapshot stats.
- Transition caveat (not a bug): on Sunday evening the Today hero still shows the *ending*
  week's legacy plan (narrative blob + "predates day-by-day sessions" + "Chili" meal) because
  today's session belongs to the old week, which was never regenerated in the new format.
  Self-heals Monday 00:00 when the new week's plan takes over. One cosmetic wrinkle noted
  below (obs-1).

### M-2 (Major) — US spellings in content → **FIXED (new content)**

- New content scanned (`/plan`, `/food`, `/checkin`): zero hits for chili/yogurt/flavor/fiber
  etc.; the home meal uses "courgette" (UK). The prompt now explicitly demands
  "chilli not chili, yoghurt not yogurt" (`lib/weeklyPlan.ts` STYLE RULES).
- The old "Chili"/"chili" strings remain visible **only** in the ending week's legacy hero on
  Today until Monday — stale data, not a fix failure.

### m-1 (Minor) — raw ISO dates → **FIXED (new content)**

- No `YYYY-MM-DD` in the visible text of `/plan` or `/food` (dates render "Monday 24 Aug").
  Prompt now bans ISO dates in text; `raceSummary` uses `formatDateShort`. The stale
  "2026-08-15" remains only in the legacy Today hero until rolloff.

### m-2 (Minor) — double-voiced Sunday state → **FIXED**

- The "Next week's plan is ready — review it" banner is gone (zero occurrences). The week
  strip heading now reads "**Next week**" with an inline "**Plan ready — review it**" link —
  single-voiced. The banner slot above the hero is now used by the Sunday check-in nudge
  ("How did this week feel? … Check in"), which is the U4 design, not a leftover.

### m-3 (Minor) — mixed-case API path returned HTML redirect → **FIXED**

- `curl -X POST …/Api/plan/generate` → **401 `{"error":"Unauthorised"}`**;
  `…/API/strava/sync` → **401 JSON**. Middleware now matches `/api/` case-insensitively.

### m-4 (Minor) — /activity 404 → **FIXED**

- `curl …/activity` → **308** with `Location: …/activities` (permanent redirect, per
  `next.config.mjs`).

### cos-1 (Cosmetic) — dormant "Error" label in Settings connections → **FIXED**

- Settings connections markup now shows only "Connected · Last synced 2 min ago" per
  provider. The remaining "error" strings in the HTML are Next.js framework internals
  (error-boundary templates, `next-error-h1` CSS) — no user-visible label.

**Round-1 verdict count: 8 of 8 FIXED. Zero STILL BROKEN.**

---

## 2. New features (fix-round scope)

### U1 — run-only running stats → **PASS**

- `/activities` chart is titled "**Weekly running km — last 8 weeks**"; totals are
  **16 km / 7 days** and **22 km / 28 days**, which exactly matches the run rows only
  (5.2 + 4.6 + 5.9 + 0.4 = 16.1; + 5.6 = 21.7) while five Rides totalling ~256 km are
  excluded — round 1 showed 212 km all-type, so the filter demonstrably bit.
- Non-runs are clearly labelled with a humanised type and **no pace**:
  "Afternoon Ride — Ride · 50.2 km · 1 h 54". Runs show pace ("5:35 /km").
  `humaniseType` in `app/activities/page.tsx` converts camel-case ids
  ("WeightTraining" → "Weight training") — only Rides were live to observe.
- Plan generation context is run-only: the fresh `week_summary` targets "roughly **20 km
  running** for the week" (consistent with the 22 km run-only history, not the 256 km of
  rides), and the prompt states "Only running counts as running volume; rides and gym work
  are supporting training". Type-aware completion ticks (`activityCategory` in
  `components/data.ts`) exist in code; no completed days in the new week yet to observe live.

### U2 — two gym-based strength sessions → **PASS (with a watch item)**

- The fresh plan contains **exactly 2** `strength` sessions: Wednesday 26 Aug ("Gym strength
  session — squats, deadlifts or leg press, rows, presses, 3 sets of 6-8") and Friday 28 Aug
  ("Gym strength session, London hotel — squats, hip hinge, push and pull"). Both are
  unambiguously **gym-based** — barbell/machine movements, "gym on site", hotel gym assumed —
  no bodyweight/hotel-room prescriptions anywhere.
- Badge and colour treatment render: "Strength" badges on both day cards, "Str" tiles in the
  Today week strip, `--s-strength` / `--s-strength-soft` defined in `app/globals.css`
  (light `#0b7285` / dark `#66d9e8`).
- **Watch item**: the sessions are Wed + Fri — not the Tue + Sat default, and both are
  weekdays (the rule wants one weekday + one weekend day). This week is a defensible
  exception: 6 of 7 days are travel-flagged, Saturday carries the 85-min long run and Sunday
  is travel-to-Lakes + shakeout, so the calendar plausibly "made those impossible". But
  enforcement in `lib/weeklyPlan.ts` is a **soft check (warn only)** — a quiet drift to
  two-weekday strength on normal weeks would not be caught. Re-check on the first
  low-travel week.

### U3 — evidence-grounded training content → **PASS (spot-check)**

- Intensity distribution: every run in the plan is easy/conversational plus one long run —
  no hard sessions in base phase after a training gap; "walk breaks fine if needed given the
  recent gap" is exactly the cautious-ramp behaviour asked for.
- Strength rationale echoes the evidence base ("Strength work builds running economy").
- No calorie-deficit language anywhere; travel-Friday advice ("eat a proper meal before
  drinks… go easy on alcohol and fried snacks") is suggestive, not preachy; fuelling stays
  qualitative ("carb-rich dinner tonight… to properly refuel"). The distilled
  `EVIDENCE_PRINCIPLES` block in `lib/weeklyPlan.ts` cites its evidence-base items in code
  comments only, per the U3 contract.

### U4 — Check-in (context & feedback) → **PASS on render and placement; submission NEEDS-HUMAN-CHECK**

- `/checkin` returns 200 (and is PIN-gated: uncookied → 307 to `/pin?next=%2Fcheckin`).
  Renders exactly per DESIGN §8: two textareas (`name="feedback"` — "How did this week
  feel?", `name="injuries"` — "Current injuries / niggles"), two Save buttons, the injuries
  read-back line ("The planner believes you are injury-free."), and the weekly note keyed to
  "**Week of 17 Aug**" — `mondayOf(today)`, the week being *described*, correctly not the
  boundary week (it flipped to 24 Aug at 17:00 today).
- Entry points all present and ≤ 2 taps: Today persistent row ("Check-in — Injuries and how
  the week felt"), Today Sunday nudge banner ("How did this week feel? A 30-second note
  shapes next week's plan — Check in"), Plan week-summary link, Settings row.
- **Submission not testable from this environment**: the forms are client components wired to
  Next server actions; the action IDs are compiled into the JS chunks (forms render
  `action=""` + multipart POST) and are not practically extractable/replayable via curl
  without risking malformed writes. **No test data was submitted — nothing to clean up.**
  The human should: type a note, Save, reload (value persists), clear it, and confirm a
  saved injury shows "Working around: …" on Today and appears in the next generated plan.
  Server-side plumbing verified in source: `app/settings/actions.ts` (`saveInjuries`,
  `saveWeeklyFeedback` with Monday-snapping and empty-string clear/delete),
  `components/data.ts` (`getRunnerContext`, `getRecentFeedback`), and the "CONTEXT FROM THE
  RUNNER" section in the generation prompt.

### Security regression quick-pass → **PASS**

- Uncookied `/` → 307 `/pin`; uncookied `POST /api/plan/generate` → 401 JSON; tampered
  cookie (`rc_auth=deadbeef`) → 401; `/manifest.json` and `/sw.js` still exempt (200);
  new `/checkin` route correctly inside the gate.

---

## 3. New findings this round

### m-5 (Minor, content) — travel-day meal is a recipe and leaks into the shopping list

- Area: §3.4 / §3.5.
- Repro: `curl -s -b rc.txt https://run-coach-fawn.vercel.app/food` — Sunday 30 Aug (a
  travel-flagged day, "Lakes"): badge "Eating out" but the meal is "**Tuna and bean salad**"
  with a cook-it-yourself method ("Drain tuna and beans into a bowl. Add halved cherry
  tomatoes…"), and its ingredients (tinned tuna 1-2 tins, tinned mixed beans 1 tin, cherry
  tomatoes 1 small punnet) appear in the **shopping list**.
- Expected: §3.4 — travel days get 1–2 sentences of *eating-out guidance*, no recipe; §3.5 —
  "travel days contribute nothing" to the shopping list.
- Actual: a recipe under an Eating-out badge, and 3 of the 9 shopping items belong to it.
  The model likely reasoned "self-catering cottage in the Lakes" (plausible in reality, but
  the spec is explicit). Validation (`coerceMeal`) forces travel meals' `ingredients` to
  empty but does not check the *shopping list* for travel-meal contamination, nor that travel
  `short_instructions` reads as guidance rather than a method.
- Likely file: `lib/weeklyPlan.ts` (prompt tightening and/or a soft check that shopping items
  trace to home/assemble meals).

### obs-1 (Cosmetic, self-resolving) — Sunday-evening legacy hero gives a dead-end instruction

- Today's hero (the ending week's legacy plan) says "regenerate from the Plan tab to get
  [day-by-day sessions]" — but from Sunday 17:00 regeneration targets *next* week, so
  following the instruction cannot upgrade the plan being displayed. Only visible for the
  few hours between the boundary flip and midnight on the last-ever legacy week; disappears
  permanently once no legacy plan is current. No action strictly needed; noting for
  completeness.

No other regressions found: pages all 200, syncs healthy ("Last synced 2 min ago", both
providers), no emoji, no calorie/macro/points numbers, no exclamation marks in generated
copy (the only `!` characters on any page are inline-JS `!==`), 24-hour times and DD MMM
dates throughout the new content.

---

## 4. REQUIREMENTS.md v1 Must-have verdict (final)

| Requirement | Verdict | Evidence |
|---|---|---|
| §3.0 Navigation | PASS | 5 tabs resolve; /checkin + /calendar as gated secondary screens |
| §3.1 PIN gate | PASS | re-verified incl. mixed-case fix and /checkin coverage |
| §3.2 Today | PASS | structured week strip, snapshot, check-in row; hero verified via new-plan data (Sunday transition renders legacy week correctly until Monday) |
| §3.3 Training plan (structured, + strength) | PASS | 7 day cards, badges, summary, phase; U2 watch item on strength day placement |
| §3.4 Meals (types) | PASS (m-5 caveat) | badges, prep, Method disclosure; one travel-day recipe slip |
| §3.5 Shopping list | PASS (m-5 caveat) | categorised, qualitative, tick counter; 3 leaked travel items |
| §3.7 Generation & limits | PASS | 200 in ~28 s; 2-min/8-day guardrails verified round 1 |
| §3.8 Sync | PASS | both providers, status surfaced, clean error slot |
| §3.9 Settings | PASS | all sections + check-in link + footer |
| §3.11 Context & feedback | PASS (render/placement) | submission E2E → human check |
| §4 Content rules | PASS | UK English, no ISO dates, no emoji/macros/exclamations in new content |
| §5.2 PWA/offline | PASS (server side) | manifest + SW unchanged and correct |
| §5.5 Europe/London time | PASS | boundary flip observed live at Sunday 17:00+; week keying correct |

---

## 5. Needs-human-check (for the real user, on the phone)

1. **Check-in submission end-to-end**: save a weekly note and an injury; reload; confirm
   persistence, the "Working around: {injuries}" read-back on Today, "Clear — all healed",
   and that the next generated plan reflects both.
2. Strength badge/tile colour rendering (teal `--s-strength`) in light and dark mode.
3. All round-1 visual/touch items: tab bar behaviour, deep links, Method disclosures,
   shopping ticks (persist/strike/reset), PIN keypad shake + countdown, skeletons, offline
   banner, pull-to-refresh, iPhone PWA install + safe areas, performance.
4. Monday morning sanity glance: confirm Today's hero now shows the structured session
   (Rest — travel to Preston) and the legacy blob is gone.
5. Next low-travel week: check strength sessions land Tue + Sat (U2 watch item).

---

Budget note: zero plan generations spent this round (3 of 8 daily remain used). No data
written, no destructive actions, no code modified.
